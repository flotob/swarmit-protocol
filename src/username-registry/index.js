/**
 * Username registry subpath — ABI, ethers Interface, event topics, and calldata encoders
 * for SwarmitUsernameRegistry.
 *
 * Like the `chain/` subpath, this does NOT provide providers or signers.
 */

export { ABI } from './abi.js';
export { iface, TOPICS } from './interface.js';
export { encode } from './encode.js';
